import type { Metadata } from "next"

export const metadata: Metadata = {
    title: "Contact | XV Kanteerava",
    description: "Get in touch with XV Kanteerava Scout Unit. Join us, volunteer, or collaborate on community service activities.",
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
    return children
}
